sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        columns:
          - name: id
            data_tests:
              - not_null
              - unique
              
        
            
      - name: orders
        columns:
          - name: id
            data_tests:
              - not_null
              - unique
              
        config:
          loaded_at_field: _ETL_LOADED_AT
          freshness: 
            warn_after: 
              count: 12
              period: hour
            error_after: 
              count: 24 
              period: hour
  